# MemoryNav — Development + Test Dependencies
# Install after requirements.txt:
#   pip install -r requirements-dev.txt

pytest==8.4.0
pytest-asyncio==0.26.0
pytest-timeout==2.4.0          # --timeout flag used in CI
httpx==0.28.1                  # FastAPI TestClient
